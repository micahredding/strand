<%= form(request.fullpath, :post) %>
<%
  if @content
	  title = @content.title || ''
	  body = @content.body || ''
	else
		title = ''
		body = ''
	end
%>
<%= input(:content, :title, :value => title) %>
<%= textarea(:content, :body, body, :rows => 20) %>
<%= submit %>
